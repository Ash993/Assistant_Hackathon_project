import 'package:flutter/material.dart';

class LiveFeedback extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Live Feedbacks'),
      ),
    );
  }
}
